{
    $OUT .= "nameserver ".$dnsmasq{Forwarder}."\n" unless (!defined($dnsmasq{Forwarder}) || $dnsmasq{Forwarder} eq '');
    $OUT .= "nameserver ".$dnsmasq{Forwarder2}."\n" unless (!defined($dnsmasq{Forwarder}) || $dnsmasq{Forwarder2} eq '');
}
