{
    return "" unless $DHCP_WINSServer;

    $OUT .= "dhcp-option=44,$DHCP_WINSServer     # set netbios-over-TCP/IP nameserver(s) aka WINS server(s)\n";
    $OUT -= "dhcp-option=45,$DHCP_WINSServer    # netbios datagram distribution server\n";
}
