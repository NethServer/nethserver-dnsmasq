{ 
    return "" unless ($dhcpd{status} eq 'enabled');

    my $router = (defined $SystemMode && $SystemMode =~ /servergateway/)
	? $LocalIP :
	    defined $GatewayIP ? $GatewayIP : undef;

    $OUT = "";
    if ($router)
    {
        $OUT .= "dhcp-option=option:router,$router\n";
    }
}
